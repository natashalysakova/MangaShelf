services:
  mangashelf:
    image: ${DOCKER_REGISTRY-}mangashelf
    build:
      context: .
      dockerfile: MangaShelf/Dockerfile

  mangashelf.api:
    image: ${DOCKER_REGISTRY-}mangashelfapi
    build:
      context: .
      dockerfile: MangaShelf.Api/Dockerfile

