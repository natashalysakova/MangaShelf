@page "/volume/{PublicId}"
@using MangaShelf.Components.Pages.VolumePage.Elements

<VolumePageContent PublicId="@PublicId" />

@code {
    [Parameter()]
    public required string PublicId { get; set; }
}
